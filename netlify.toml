[build]
  command = "npm install"
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Nudge checkouts that were started but never paid. One email per checkout, ever.
[functions."checkout-reminders"]
  schedule = "@hourly"
